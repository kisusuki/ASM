<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="utf-8">
   <meta name="viewport" content="width=device-width, initial-scale=1">
   <title>Ecommerce Navbar Design</title>

   <link href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
   <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet">
   <link rel="stylesheet" href="style.css">
</head>

<body>
   <div class="border">
   <div class="container">
      <div class="row">
         {{#each toys}}
         <div class="col-md-4">
            <div class="product-card">
               <div class="product-card-img">
                  <img class="card-img-top mt-3" src="{{ image }}" width="200" height="200">
               </div>
               <div class="product-card-body">
                  <div>
                     <h4 class="card-title text-success">{{ name }}</h4>
                     <h3 class="card-title text-danger">${{ price }}</h3>
                     <p class="fw-bold card-text text-primary">{{ brand }}</p>
                     <h6>{{ detail }}</h6> 
                  </div>
                  <form action="/order" method="post">
                     <input type="hidden" name="id" value="{{_id}}">
                     <input type="hidden" name="price" value="{{price}}">
                     <input class="form-control mb-3" type="number" name="order_quantity" min="1" max="{{quantity}}">
                     <input class="btn btn-danger" type="submit" value="Add To Cart">
                  </form>
               </div>
            </div>
         </div>
         {{/each}}
      </div>
   </div>
</div>

   {{!-- <div  class="border" class="card-group text-center">
      <div class="container">
         <div class="row">
            {{#each toys}}
            <div class="card-group text-center">
               <h4 class="mb-4">Our Products</h4>
            </div>
            <div class="col-md-3">
               <div class="product-card">
                  <div class="product-card-img">
                     <img class="card-img-top mt-3" src="{{ image }}" width="200" height="200">
                  </div>
                  <div class="product-card-body">
                     <div>
                        <h4 class="card-title text-success">{{ name }}</h4>
                        <h3 class="card-title text-danger">${{ price }}</h3>
                        <p class="fw-bold card-text text-primary">{{ brand }}</p>
                        <h6>{{ detail }}</h3>
                     </div>
                     <form action="/order" method="post">
                        <input type="hidden" name="id" value="{{_id}}">
                        <input type="hidden" name="price" value="{{price}}">
                        <input class="form-control mb-3" type="number" name="order_quantity" min="1" max="{{quantity}}">
                        <input class="btn btn-danger" type="submit" value="Order">
                     </form>
                  </div>
               </div>
            </div>
            {{/each}}
         </div>

      </div>

   </div> --}}
   {{!-- <div class="card-group text-center" class="border">
      <div class="row">
         {{#each toys }}
         <div class="card col-md-3 m-3">
            <img class="card-img-top mt-3" src="{{ image }}" width="200" height="200">
            <div class="card-body">
               <h4 class="card-title text-success">{{ name }}</h4>
               <h3 class="card-title text-danger">${{ price }}</h3>
               <p class="fw-bold card-text text-primary">{{ brand }}</p>
               <h6>{{ detail }}</h3>
                  <form action="/order" method="post">
                     <input type="hidden" name="id" value="{{_id}}">
                     <input type="hidden" name="price" value="{{price}}">
                     <input class="form-control mb-3" type="number" name="order_quantity" min="1" max="{{quantity}}">
                     <input class="btn btn-danger" type="submit" value="Order">
                  </form>
            </div>
         </div>
         {{/each}}
      </div>
   </div> --}}

</body>

</html>